<h1 class="text-2xl font-bold text-gray-900 mb-4">Golfers</h1>

<ul class="list-disc list-inside space-y-2 mb-6">
  <% @golfers.each do |g| %>
    <li><%= g.name %><%= " (ID: #{g.external_id})" if g.external_id.present? %></li>
  <% end %>
</ul>
<% if @golfers.empty? %>
  <p class="text-gray-600 mb-6">No golfers yet. Sync players from the API or add them via sync.</p>
<% end %>
